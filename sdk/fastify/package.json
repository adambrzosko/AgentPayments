{
  "name": "@agentpayments/fastify",
  "version": "0.1.0",
  "description": "AgentPayments gate for Fastify — same gate behavior as @agentpayments/node, wired in via the official Express-compat layer",
  "license": "MIT",
  "main": "index.js",
  "types": "index.d.ts",
  "files": [
    "index.js",
    "index.d.ts",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "agentpayments",
    "ai-agents",
    "solana",
    "usdc",
    "payments",
    "fastify",
    "x402"
  ],
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@agentpayments/node": "^0.1.1",
    "@fastify/express": "^4.0.7",
    "fastify-plugin": "^6.0.0"
  },
  "peerDependencies": {
    "fastify": "^4.0.0 || ^5.0.0"
  },
  "devDependencies": {
    "fastify": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adambrzosko/AgentPayments.git"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  }
}
